name: Empty Workflow

on:
  workflow_dispatch

jobs:
  placeholder:
    runs-on: ubuntu-latest
    environment:
      name: require-approval
    steps:
      - name: Test
        run: echo "This is a test."
